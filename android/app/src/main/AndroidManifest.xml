<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Retoma o rastreio depois que o celular reinicia, sem precisar o
             fiscal abrir o app manualmente — só religa se houver login
             recente com sessão ainda válida (ver RastreioBootReceiver). -->
        <receiver
            android:name="com.equimaps.capacitor_background_geolocation.RastreioBootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Rastreio em segundo plano (Fiscais em Campo). As permissões de
         localização/serviço em primeiro plano/notificação já vêm do
         manifesto do plugin @capacitor-community/background-geolocation
         (merge automático do Gradle) — só ACCESS_BACKGROUND_LOCATION
         precisa ser declarada explicitamente pelo app consumidor. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Permite pedir ao usuário, num único diálogo do próprio Android, pra
         ignorar a otimização de bateria PADRÃO do sistema (Doze) pro app —
         ver requestIgnoreBatteryOptimizations() em rastreio.js. Não cobre
         gerenciadores de bateria proprietários de fabricante (MIUI etc). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Permite que o RastreioBootReceiver seja acordado quando o celular
         termina de reiniciar, pra retomar o rastreio sem depender do fiscal
         abrir o app manualmente (ver seção 4.1 da especificação do Codex). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
